# 复制为 .env.production 或在构建环境中导出后再 build：
NEXT_PUBLIC_AI_SERVICE_URL=http://whale3070.com:8787

# 工作台「AI 聊天」：OpenAI 兼容 chat/completions（如 HopeAI / OneAPI）
# CLAUDE_URL=https://api.example.com/v1/chat/completions
# CLAUDE_API=sk-...
# CLAUDE_MODEL=claude-opus-4-6
# 推荐：在配置 CLAUDE_URL + CLAUDE_API 后，用 CLAUDE_MODEL_ID1、ID2、ID3… 列出可选模型（同一上游，仅 id 不同）；工作台为下拉选择，可续写 ID4 等
# 若未设 CLAUDE_MODEL_ID1，则仍用 CLAUDE_MODEL，且可选用 CLAUDE_MODEL_ID2 作第二路（旧方式）
# CLAUDE_MODEL_ID1=claude-opus-4-thinking-all
# CLAUDE_MODEL_ID2=claude-sonnet-4-thinking-all
# CLAUDE_MODEL_ID3=claude-sonnet-4-6
# 默认 max_tokens=4096；可下调或上调到 8192
# CLAUDE_MAX_TOKENS=4096
# 可选：完全自定义 system 提示（不设则使用内置：说明不能联网、只能根据粘贴文本等）
# CLAUDE_SYSTEM=Your system prompt in English or 中文.
